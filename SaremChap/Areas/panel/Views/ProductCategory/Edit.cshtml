@model Domainclasses.Modes.ProductCategory

<div class="call-action call-action-boxed call-action-style2 clearfix text-right">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-vertical">

            @Html.ValidationSummary(true)
            @Html.HiddenFor(model => model.ProductCategoryID)

            <div class="form-group">
                <div class="col-md-6">
                    @Html.TextBoxFor(model => model.name, new { @class = "form-control", placeholder = "نام گروه", style = "text-align:right" })
                    @Html.ValidationMessageFor(model => model.name)
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-6">
                    @Html.EnumDropDownListFor(model => model.SuperCategory, "--انتخاب کنید--", new { @class = "form-control", style = "text-align:right" })                    @Html.ValidationMessageFor(model => model.imege)
                    @Html.ValidationMessageFor(model => model.SuperCategory)
                </div>
            </div>
            <br />
            <div class="form-group">
                <div class="container">
                    <div class="row">
                        <div class="col-md-2">
                            <div class="input-group">
                                <img class="img-thumbnail" id="preview-image" src="@Model.imege" width="128" style="cursor:pointer" />
                                <input type="hidden" class="image" id="imege" name="imege" value="@Model.imege" />
                            </div>
                            @Html.ValidationMessageFor(model => model.imege)
                        </div>
                        <div class="col-md-10" id="imagelist" style="display:none">
                            @{Html.RenderAction("imageList", "GalleryItem", new { Area = "panel" });}
                        </div>
                    </div>
                </div>
            </div>

        </div>

      
        <div class="form-group">
            <div class="col-md-12">
                @Html.TextAreaFor(model => model.describtion, new { @class = "form-control", placeholder = "توضاحات", style = "text-align:right", id = "Content" })
                @Html.ValidationMessageFor(model => model.describtion)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-12">
                <input type="submit" value="  ثبت  " class="btn-system btn-large" />
            </div>
        </div>
    }

</div>
<script src="~/Content/plugins/ckeditor/ckeditor.js"></script>

<script>
    if (CKEDITOR.env.ie && CKEDITOR.env.version < 9)
        CKEDITOR.tools.enableHtml5Elements(document);
    CKEDITOR.config.height = 150;
    CKEDITOR.config.width = 'auto';
    CKEDITOR.config.language = 'fa';
    CKEDITOR.config.skin = 'office2013';
    var initSample = (function () {
        var wysiwygareaAvailable = isWysiwygareaAvailable(),
            isBBCodeBuiltIn = !!CKEDITOR.plugins.get('bbcode');

        return function () {
            var editorElement = CKEDITOR.document.getById('Content');

            if (wysiwygareaAvailable) {
                CKEDITOR.replace('Content');
            } else {
                editorElement.setAttribute('contenteditable', 'true');
                CKEDITOR.inline('Content');
            }
        };

        function isWysiwygareaAvailable() {
            if (CKEDITOR.revision == ('%RE' + 'V%')) {
                return true;
            }

            return !!CKEDITOR.plugins.get('wysiwygarea');
        }
    })();

    initSample();

    $("#preview-image").click(function () {
        $("#imagelist").fadeToggle();
    })

    $(document).ready(function () {
        $("form").validetta();
    })
    $('input').attr('data-validetta', 'required');
    $('textarea').attr('data-validetta', 'required');
    $('select').attr('data-validetta', 'required');
    $("select").addClass("form-control")


</script>
